============
Installation
============

At the command line::

    $ pip install tap-as-a-service

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv tap-as-a-service
    $ pip install tap-as-a-service
